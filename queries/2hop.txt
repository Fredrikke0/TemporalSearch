BENCHMARK_TYPE: 2HOP
# ordinal us pres and party
#SELECT q1.ordinal, q1.pres, q2.party FROM source ALIAS q1 JOIN (SELECT party, COUNT(*) FROM source WHERE (CONTAINS('democrat') BIND party OR CONTAINS('republican') BIND party) AND NER(PERSON) BIND person GRANULARITY SENTENCE) ALIAS q2 ON q1.pres = q2.person WHERE DEPENDS('president','amod', '38th') AND CONTAINS('united states') AND NER(ORDINAL) BIND ordinal AND NER(PERSON) BIND pres GRANULARITY SENTENCE
#SELECT q1.ordinal, q1.pres, q2.party FROM source ALIAS q1 JOIN (SELECT party, COUNT(*) FROM source WHERE (CONTAINS('democrat') BIND party OR CONTAINS('republican') BIND party) AND NER(PERSON) BIND person GRANULARITY SENTENCE) ALIAS q2 ON q1.pres = q2.person WHERE DEPENDS('president','amod', '39th') AND CONTAINS('united states') AND NER(ORDINAL) BIND ordinal AND NER(PERSON) BIND pres GRANULARITY SENTENCE
#SELECT q1.ordinal, q1.pres, q2.party FROM source ALIAS q1 JOIN (SELECT party, COUNT(*) FROM source WHERE (CONTAINS('democrat') BIND party OR CONTAINS('republican') BIND party) AND NER(PERSON) BIND person GRANULARITY SENTENCE) ALIAS q2 ON q1.pres = q2.person WHERE DEPENDS('president','amod', '14th') AND CONTAINS('united states') AND NER(ORDINAL) BIND ordinal AND NER(PERSON) BIND pres GRANULARITY SENTENCE

# List of presidents before/after president.
SELECT q2.p, q2.date, COUNT(*) FROM articles ALIAS q1 JOIN (SELECT p, date FROM wiki WHERE CONTAINS('president') AND CONTAINS('united states') AND NER(PERSON) BIND p AND DATE(< 2020) BIND date AND NOT(NER(PERSON, 'reagan')) GROUP BY p, date GRANULARITY SENTENCE) ALIAS q2 ON q1.date BEFORE q2.date WHERE NER(PERSON, 'reagan') BIND p AND CONTAINS('president') AND CONTAINS('united states') AND DATE(< 2050) BIND date GROUP BY q2.date, q2.p GRANULARITY SENTENCE ORDER BY COUNT(*) DESC
SELECT q2.p, q2.date, COUNT(*) FROM articles ALIAS q1 JOIN (SELECT p, date FROM wiki WHERE CONTAINS('president') AND CONTAINS('united states') AND NER(PERSON) BIND p AND DATE(< 2020) BIND date AND NOT(NER(PERSON, 'clinton')) GROUP BY p, date GRANULARITY SENTENCE) ALIAS q2 ON q1.date BEFORE q2.date WHERE NER(PERSON, 'clinton') BIND p AND CONTAINS('president') AND CONTAINS('united states') AND DATE(< 2050) BIND date GROUP BY q2.date, q2.p GRANULARITY SENTENCE ORDER BY COUNT(*) DESC
SELECT q2.p, q2.date, COUNT(*) FROM articles ALIAS q1 JOIN (SELECT p, date FROM wiki WHERE CONTAINS('president') AND CONTAINS('united states') AND NER(PERSON) BIND p AND DATE(< 2020) BIND date AND NOT(NER(PERSON, 'reagan')) GROUP BY p, date GRANULARITY SENTENCE) ALIAS q2 ON q1.date AFTER q2.date WHERE NER(PERSON, 'reagan') BIND p AND CONTAINS('president') AND CONTAINS('united states') AND DATE(< 2050) BIND date GROUP BY q2.date, q2.p GRANULARITY SENTENCE ORDER BY COUNT(*) DESC
SELECT q2.p, q2.date, COUNT(*) FROM articles ALIAS q1 JOIN (SELECT p, date FROM wiki WHERE CONTAINS('president') AND CONTAINS('united states') AND NER(PERSON) BIND p AND DATE(< 2020) BIND date AND NOT(NER(PERSON, 'clinton')) GROUP BY p, date GRANULARITY SENTENCE) ALIAS q2 ON q1.date AFTER q2.date WHERE NER(PERSON, 'clinton') BIND p AND CONTAINS('president') AND CONTAINS('united states') AND DATE(< 2050) BIND date GROUP BY q2.date, q2.p GRANULARITY SENTENCE ORDER BY COUNT(*) DESC

# List of unemployment rates joined on current president
SELECT q1.country, q1.rate, q1.date, q2.president, COUNT(q2.president) FROM articles ALIAS q1 JOIN (SELECT date, president FROM wiki WHERE CONTAINS('president') AND NER(COUNTRY, 'united states')  AND DATE(< 2020) BIND date AND NER(PERSON) BIND president GROUP BY president, date GRANULARITY SENTENCE) ALIAS q2 ON q1.date = q2.date WHERE CONTAINS('unemployment') AND NER(COUNTRY, 'united states') BIND country AND NER(PERCENT) BIND rate AND DATE(< 2025) BIND date GROUP BY q1.country, q1.rate, q1.date, q2.president GRANULARITY SENTENCE ORDER BY COUNT(q2.president) DESC
SELECT q1.country, q1.rate, q1.date, q2.president, COUNT(q2.president) FROM articles ALIAS q1 JOIN (SELECT date, president FROM wiki WHERE CONTAINS('president') AND NER(COUNTRY, 'france')  AND DATE(< 2020) BIND date AND NER(PERSON) BIND president GROUP BY president, date GRANULARITY SENTENCE) ALIAS q2 ON q1.date = q2.date WHERE CONTAINS('unemployment') AND NER(COUNTRY, 'france') BIND country AND NER(PERCENT) BIND rate AND DATE(< 2025) BIND date GROUP BY q1.country, q1.rate, q1.date, q2.president GRANULARITY SENTENCE ORDER BY COUNT(q2.president) DESC
SELECT q1.country, q1.rate, q1.date, q2.president, COUNT(q2.president) FROM articles ALIAS q1 JOIN (SELECT date, president FROM wiki WHERE CONTAINS('chancellor') AND NER(COUNTRY, 'germany')  AND DATE(< 2020) BIND date AND NER(PERSON) BIND president GROUP BY president, date GRANULARITY SENTENCE) ALIAS q2 ON q1.date = q2.date WHERE CONTAINS('unemployment') AND NER(COUNTRY, 'germany') BIND country AND NER(PERCENT) BIND rate AND DATE(< 2025) BIND date GROUP BY q1.country, q1.rate, q1.date, q2.president GRANULARITY SENTENCE ORDER BY COUNT(q2.president) DESC

# List of astronauts after Armstrong walked on the moon
SELECT q2.person, q2.date, COUNT(*) FROM source ALIAS q2 JOIN (SELECT date, person FROM source WHERE CONTAINS('moon') AND NER(PERSON, 'Neil Armstrong') BIND person AND DATE(< 2050) BIND date GROUP BY person, date GRANULARITY SENTENCE) ALIAS q1 ON q2.date AFTER q1.date WHERE CONTAINS('astronaut') AND NER(PERSON) BIND person AND DATE(< 2050) BIND date GROUP BY q2.person, q2.date GRANULARITY SENTENCE ORDER BY COUNT(*)

# Nobel prize winners and nationality
SELECT q1.nobelWinner, q1.nobelDate, q2.countryName, COUNT(q1.nobelWinner) FROM articles ALIAS q1 JOIN (SELECT personName, countryName FROM articles WHERE NER(NATIONALITY) BIND countryName AND NER(PERSON) BIND personName GROUP BY personName, countryName GRANULARITY SENTENCE) ALIAS q2 ON q1.nobelWinner = q2.personName WHERE CONTAINS('Nobel Prize') AND NER(PERSON) BIND nobelWinner AND DATE(AFTER 2005) BIND nobelDate GROUP BY q1.nobelWinner, q1.nobelDate, q2.countryName GRANULARITY SENTENCE ORDER BY COUNT(q1.nobelWinner) DESC
SELECT q1.nobelWinner, q1.nobelDate, q2.countryName, COUNT(q1.nobelWinner) FROM articles ALIAS q1 JOIN (SELECT personName, countryName FROM articles WHERE NER(NATIONALITY) BIND countryName AND NER(PERSON) BIND personName GROUP BY personName, countryName GRANULARITY SENTENCE) ALIAS q2 ON q1.nobelWinner = q2.personName WHERE CONTAINS('Nobel Prize') AND NER(PERSON) BIND nobelWinner AND DATE(BEFORE 2005) BIND nobelDate GROUP BY q1.nobelWinner, q1.nobelDate, q2.countryName GRANULARITY SENTENCE ORDER BY COUNT(q1.nobelWinner) DESC
SELECT q1.nobelWinner, q1.nobelDate, q2.countryName, COUNT(q1.nobelWinner) FROM articles ALIAS q1 JOIN (SELECT personName, countryName FROM articles WHERE NER(NATIONALITY) BIND countryName AND NER(PERSON) BIND personName GROUP BY personName, countryName GRANULARITY SENTENCE) ALIAS q2 ON q1.nobelWinner = q2.personName WHERE CONTAINS('Nobel Prize') AND NER(PERSON) BIND nobelWinner AND DATE(= 2004) BIND nobelDate GROUP BY q1.nobelWinner, q1.nobelDate, q2.countryName GRANULARITY SENTENCE ORDER BY COUNT(q1.nobelWinner) DESC
SELECT q1.nobelWinner, q1.nobelDate, q2.countryName, COUNT(q1.nobelWinner) FROM articles ALIAS q1 JOIN (SELECT personName, countryName FROM articles WHERE NER(NATIONALITY) BIND countryName AND NER(PERSON) BIND personName GROUP BY personName, countryName GRANULARITY SENTENCE) ALIAS q2 ON q1.nobelWinner = q2.personName WHERE CONTAINS('Nobel Prize') AND NER(PERSON) BIND nobelWinner AND DATE(= 2001) BIND nobelDate GROUP BY q1.nobelWinner, q1.nobelDate, q2.countryName GRANULARITY SENTENCE ORDER BY COUNT(q1.nobelWinner) DESC
SELECT q1.nobelWinner, q1.nobelDate, q2.countryName, COUNT(q1.nobelWinner) FROM articles ALIAS q1 JOIN (SELECT personName, countryName FROM articles WHERE NER(NATIONALITY) BIND countryName AND NER(PERSON) BIND personName GROUP BY personName, countryName GRANULARITY SENTENCE) ALIAS q2 ON q1.nobelWinner = q2.personName WHERE CONTAINS('Nobel Prize') AND NER(PERSON) BIND nobelWinner AND DATE(= 1994) BIND nobelDate GROUP BY q1.nobelWinner, q1.nobelDate, q2.countryName GRANULARITY SENTENCE ORDER BY COUNT(q1.nobelWinner) DESC
SELECT q1.nobelWinner, q1.nobelDate, q2.countryName, COUNT(q1.nobelWinner) FROM articles ALIAS q1 JOIN (SELECT personName, countryName FROM articles WHERE NER(NATIONALITY) BIND countryName AND NER(PERSON) BIND personName GROUP BY personName, countryName GRANULARITY SENTENCE) ALIAS q2 ON q1.nobelWinner = q2.personName WHERE CONTAINS('Nobel Prize') AND NER(PERSON) BIND nobelWinner AND DATE(= 1998) BIND nobelDate GROUP BY q1.nobelWinner, q1.nobelDate, q2.countryName GRANULARITY SENTENCE ORDER BY COUNT(q1.nobelWinner) DESC

# Nobel Laureates of 1998 and their Associated Organizations
SELECT q1.personName, q1.event_date, q2.organizationName, COUNT(*) FROM articles ALIAS q1 JOIN (SELECT personName, organizationName FROM articles WHERE CONTAINS('Nobel Prize') AND NER(ORGANIZATION) BIND organizationName AND NER(PERSON) BIND personName GROUP BY personName, organizationName GRANULARITY SENTENCE) ALIAS q2 ON q1.personName = q2.personName WHERE CONTAINS('Nobel Prize') AND NER(PERSON) BIND personName AND DATE(= 1998) BIND event_date GROUP BY q1.personName, q1.event_date, q2.organizationName GRANULARITY SENTENCE ORDER BY COUNT(*) DESC
# Nobel Laureates of 1994 and their Associated Organizations
SELECT q1.personName, q1.event_date, q2.organizationName, COUNT(*) FROM articles ALIAS q1 JOIN (SELECT personName, organizationName FROM articles WHERE CONTAINS('Nobel Prize') AND NER(ORGANIZATION) BIND organizationName AND NER(PERSON) BIND personName GROUP BY personName, organizationName GRANULARITY SENTENCE) ALIAS q2 ON q1.personName = q2.personName WHERE CONTAINS('Nobel Prize') AND NER(PERSON) BIND personName AND DATE(= 1994) BIND event_date GROUP BY q1.personName, q1.event_date, q2.organizationName GRANULARITY SENTENCE ORDER BY COUNT(*) DESC
# Nobel Laureates of 2001 and their Associated Organizations
SELECT q1.personName, q1.event_date, q2.organizationName, COUNT(*) FROM articles ALIAS q1 JOIN (SELECT personName, organizationName FROM articles WHERE CONTAINS('Nobel Prize') AND NER(ORGANIZATION) BIND organizationName AND NER(PERSON) BIND personName GROUP BY personName, organizationName GRANULARITY SENTENCE) ALIAS q2 ON q1.personName = q2.personName WHERE CONTAINS('Nobel Prize') AND NER(PERSON) BIND personName AND DATE(= 2001) BIND event_date GROUP BY q1.personName, q1.event_date, q2.organizationName GRANULARITY SENTENCE ORDER BY COUNT(*) DESC

#
SELECT q2.personName, q2.awardDate, COUNT(*) FROM articles ALIAS q1 JOIN (SELECT personName, awardDate FROM articles WHERE CONTAINS('Nobel Prize') AND NER(PERSON) BIND personName AND DATE(< 2050) BIND awardDate GRANULARITY SENTENCE) ALIAS q2 ON q1.specificWinnerDate BEFORE q2.awardDate WHERE CONTAINS('Nobel Prize') AND NER(PERSON, 'Albert Einstein') BIND specificWinnerName AND DATE(< 2050) BIND specificWinnerDate GROUP BY q2.personName, q2.awardDate GRANULARITY SENTENCE ORDER BY COUNT(*) DESC
SELECT q2.personName, q2.awardDate, COUNT(*) FROM articles ALIAS q1 JOIN (SELECT personName, awardDate FROM articles WHERE CONTAINS('Nobel Prize') AND NER(PERSON) BIND personName AND DATE(< 2050) BIND awardDate GRANULARITY SENTENCE) ALIAS q2 ON q1.specificWinnerDate BEFORE q2.awardDate WHERE NER(PERSON, 'Elie Wiesel') BIND specificWinnerName AND CONTAINS('Nobel Prize') AND DATE(< 2050) BIND specificWinnerDate GROUP BY q2.personName, q2.awardDate GRANULARITY SENTENCE ORDER BY COUNT(*)


# earthquake
SELECT q1.quakeLocation, q1.eventDate, q2.populationFigure, COUNT(*) FROM articles ALIAS q1 JOIN (SELECT locationName, populationFigure FROM articles WHERE DEPENDS('residents', 'nummod', *) BIND populationFigure AND NER(LOCATION) BIND locationName GROUP BY locationName, populationFigure GRANULARITY SENTENCE) ALIAS q2 ON q1.quakeLocation = q2.locationName WHERE CONTAINS('earthquake') AND NER(LOCATION) BIND quakeLocation AND DATE(INTERSECT [1990,1995]) BIND eventDate GROUP BY q1.quakeLocation, q1.eventDate, q2.populationFigure GRANULARITY SENTENCE ORDER BY COUNT(*) DESC
SELECT q1.quakeLocation, q1.eventDate, q2.populationFigure, COUNT(*) FROM articles ALIAS q1 JOIN (SELECT locationName, populationFigure FROM articles WHERE DEPENDS('residents', 'nummod', *) BIND populationFigure AND NER(CITY) BIND locationName GROUP BY locationName, populationFigure GRANULARITY SENTENCE) ALIAS q2 ON q1.quakeLocation = q2.locationName WHERE CONTAINS('earthquake') AND NER(CITY) BIND quakeLocation AND DATE(INTERSECT [1990,1995]) BIND eventDate GROUP BY q1.quakeLocation, q1.eventDate, q2.populationFigure GRANULARITY SENTENCE ORDER BY COUNT(*) DESC
SELECT q1.quakeLocation, q1.eventDate, q2.populationFigure, COUNT(*) FROM articles ALIAS q1 JOIN (SELECT locationName, populationFigure FROM articles WHERE DEPENDS('residents', 'nummod', *) BIND populationFigure AND NER(COUNTRY) BIND locationName GROUP BY locationName, populationFigure GRANULARITY SENTENCE) ALIAS q2 ON q1.quakeLocation = q2.locationName WHERE CONTAINS('earthquake') AND NER(COUNTRY) BIND quakeLocation AND DATE(INTERSECT [1980,1985]) BIND eventDate GROUP BY q1.quakeLocation, q1.eventDate, q2.populationFigure GRANULARITY SENTENCE ORDER BY COUNT(*) DESC
SELECT q1.quakeLocation, q1.eventDate, q2.populationFigure, COUNT(*) FROM articles ALIAS q1 JOIN (SELECT locationName, populationFigure FROM articles WHERE DEPENDS('residents', 'nummod', *) BIND populationFigure AND NER(CITY) BIND locationName GROUP BY locationName, populationFigure GRANULARITY SENTENCE) ALIAS q2 ON q1.quakeLocation = q2.locationName WHERE CONTAINS('earthquake') AND NER(CITY) BIND quakeLocation AND DATE(INTERSECT [1990,1985]) BIND eventDate GROUP BY q1.quakeLocation, q1.eventDate, q2.populationFigure GRANULARITY SENTENCE ORDER BY COUNT(*) DESC
SELECT q1.quakeLocation, q1.eventDate, q2.populationFigure, COUNT(*) FROM articles ALIAS q1 JOIN (SELECT locationName, populationFigure FROM articles WHERE DEPENDS('residents', 'nummod', *) BIND populationFigure AND NER(CITY) BIND locationName GROUP BY locationName, populationFigure GRANULARITY SENTENCE) ALIAS q2 ON q1.quakeLocation = q2.locationName WHERE CONTAINS('earthquake') AND NER(CITY) BIND quakeLocation AND DATE(INTERSECT [1995,2000]) BIND eventDate GROUP BY q1.quakeLocation, q1.eventDate, q2.populationFigure GRANULARITY SENTENCE ORDER BY COUNT(*) DESC
