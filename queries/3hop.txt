BENCHMARK_TYPE: 3HOP
# president, date and party
SELECT q1.country, q1.rate, q1.date, q2.president, COUNT(q2.president) FROM articles ALIAS q1 JOIN ( SELECT date, president FROM wiki WHERE CONTAINS('president') AND NER(COUNTRY, 'united states') AND DATE(< 2020) BIND date AND NER(PERSON) BIND president GROUP BY president, date GRANULARITY SENTENCE ) ALIAS q2 ON q1.date = q2.date JOIN ( SELECT president FROM wiki WHERE CONTAINS('president') AND CONTAINS('Republican Presidential nomination') AND NER(PERSON) BIND president GROUP BY president GRANULARITY SENTENCE ) ALIAS q3 ON q2.president = q3.president WHERE CONTAINS('unemployment') AND NER(COUNTRY, 'united states') BIND country AND NER(PERCENT) BIND rate AND DATE(< 2025) BIND date GROUP BY q1.country, q1.rate, q1.date, q2.president GRANULARITY SENTENCE ORDER BY COUNT(q2.president) DESC
# Inflation, US President, Democratic Nomination
SELECT q1.country, q1.rate, q1.date, q2.president, COUNT(q2.president) FROM articles ALIAS q1 JOIN ( SELECT date, president FROM wiki WHERE CONTAINS('president') AND NER(COUNTRY, 'united states') AND DATE(< 2020) BIND date AND NER(PERSON) BIND president GROUP BY president, date GRANULARITY SENTENCE ) ALIAS q2 ON q1.date = q2.date JOIN ( SELECT president FROM wiki WHERE CONTAINS('president') AND CONTAINS('Democratic Presidential nomination') AND NER(PERSON) BIND president GROUP BY president GRANULARITY SENTENCE ) ALIAS q3 ON q2.president = q3.president WHERE CONTAINS('inflation') AND NER(COUNTRY, 'united states') BIND country AND NER(PERCENT) BIND rate AND DATE(< 2025) BIND date GROUP BY q1.country, q1.rate, q1.date, q2.president GRANULARITY SENTENCE ORDER BY COUNT(q2.president) DESC
# Unemployment, French President, party affiliation
SELECT q1.country, q1.rate, q1.date, q2.president, q3.party, COUNT(q2.president) FROM articles ALIAS q1 JOIN ( SELECT date, president FROM wiki WHERE CONTAINS('president') AND NER(COUNTRY, 'france') AND DATE(< 2020) BIND date AND NER(PERSON) BIND president GROUP BY president, date GRANULARITY SENTENCE ) ALIAS q2 ON q1.date = q2.date JOIN ( SELECT president, party FROM wiki WHERE (CONTAINS('Socialist Party') BIND party OR CONTAINS('Gaullist') BIND party OR CONTAINS('En Marche') BIND party ) AND NER(PERSON) BIND president GROUP BY president GRANULARITY SENTENCE ) ALIAS q3 ON q2.president = q3.president WHERE CONTAINS('unemployment') AND NER(COUNTRY, 'france') BIND country AND NER(PERCENT) BIND rate AND DATE(< 2025) BIND date GROUP BY q1.country, q1.rate, q1.date, q2.president, q3.party GRANULARITY SENTENCE ORDER BY COUNT(q2.president) DESC

# 16th US President, Party, Inauguration Date
SELECT q1.ordinal, q1.pres, q2.party, q3.date, COUNT(q3.date) FROM source ALIAS q1 JOIN (SELECT party, COUNT(*) FROM source WHERE (CONTAINS('democrat') BIND party OR CONTAINS('republican') BIND party OR CONTAINS('whig')) AND NER(PERSON) BIND person GRANULARITY SENTENCE) ALIAS q2 ON q1.pres = q2.person JOIN (SELECT TITLE, person, date FROM source WHERE (CONTAINS('inaugurated') OR CONTAINS('took office')) AND NER(PERSON) BIND person AND DATE(< 1900) BIND date GRANULARITY SENTENCE) ALIAS q3 ON q2.person = q3.person WHERE DEPENDS('president','amod', '16th') AND CONTAINS('united states') AND NER(ORDINAL) BIND ordinal AND NER(PERSON) BIND pres GROUP BY q1.ordinal, q1.pres, q2.party, q3.date GRANULARITY SENTENCE
# nth US President, Party, Inauguration Date (simplified event search)
SELECT q1.ordinal, q1.pres, q2.party, q3.date, COUNT(q3.date) FROM source ALIAS q1 JOIN (SELECT party, COUNT(*) FROM source WHERE (CONTAINS('democrat') BIND party OR CONTAINS('republican') BIND party) AND NER(PERSON) BIND person GRANULARITY SENTENCE) ALIAS q2 ON q1.pres = q2.person JOIN (SELECT TITLE, person, date FROM source WHERE (CONTAINS('inaugurated') OR CONTAINS('took office')) AND NER(PERSON) BIND person AND DATE(< 1900) BIND date GRANULARITY SENTENCE) ALIAS q3 ON q2.person = q3.person WHERE DEPENDS('president','amod', '17th') AND CONTAINS('united states') AND NER(ORDINAL) BIND ordinal AND NER(PERSON) BIND pres GROUP BY q1.ordinal, q1.pres, q2.party, q3.date GRANULARITY SENTENCE
SELECT q1.ordinal, q1.pres, q2.party, q3.date, COUNT(q3.date) FROM source ALIAS q1 JOIN (SELECT party, COUNT(*) FROM source WHERE (CONTAINS('democrat') BIND party OR CONTAINS('republican') BIND party) AND NER(PERSON) BIND person GRANULARITY SENTENCE) ALIAS q2 ON q1.pres = q2.person JOIN (SELECT TITLE, person, date FROM source WHERE (CONTAINS('inaugurated') OR CONTAINS('took office')) AND NER(PERSON) BIND person AND DATE(INTERSECT [1900, 2000]) BIND date GRANULARITY SENTENCE) ALIAS q3 ON q2.person = q3.person WHERE DEPENDS('president','amod', '32nd') AND CONTAINS('united states') AND NER(ORDINAL) BIND ordinal AND NER(PERSON) BIND pres GROUP BY q1.ordinal, q1.pres, q2.party, q3.date GRANULARITY SENTENCE
SELECT q1.ordinal, q1.pres, q2.party, q3.date, COUNT(q3.date) FROM source ALIAS q1 JOIN (SELECT party, COUNT(*) FROM source WHERE (CONTAINS('democrat') BIND party OR CONTAINS('republican') BIND party) AND NER(PERSON) BIND person GRANULARITY SENTENCE) ALIAS q2 ON q1.pres = q2.person JOIN (SELECT TITLE, person, date FROM source WHERE (CONTAINS('inaugurated') OR CONTAINS('took office')) AND NER(PERSON) BIND person AND DATE(INTERSECT [1900, 2000]) BIND date GRANULARITY SENTENCE) ALIAS q3 ON q2.person = q3.person WHERE DEPENDS('president','amod', '35th') AND CONTAINS('united states') AND NER(ORDINAL) BIND ordinal AND NER(PERSON) BIND pres GROUP BY q1.ordinal, q1.pres, q2.party, q3.date GRANULARITY SENTENCE
SELECT q1.ordinal, q1.pres, q2.party, q3.date, COUNT(q3.date) FROM source ALIAS q1 JOIN (SELECT party, COUNT(*) FROM source WHERE (CONTAINS('republican') BIND party OR CONTAINS('democrat') BIND party) AND NER(PERSON) BIND person GRANULARITY SENTENCE) ALIAS q2 ON q1.pres = q2.person JOIN (SELECT TITLE, person, date FROM source WHERE (CONTAINS('inaugurated') OR CONTAINS('took office')) AND NER(PERSON) BIND person AND DATE(INTERSECT [1900, 2000]) BIND date GRANULARITY SENTENCE) ALIAS q3 ON q2.person = q3.person WHERE DEPENDS('president','amod', '37th') AND CONTAINS('united states') AND NER(ORDINAL) BIND ordinal AND NER(PERSON) BIND pres GROUP BY q1.ordinal, q1.pres, q2.party, q3.date GRANULARITY SENTENCE
SELECT q1.ordinal, q1.pres, q2.party, q3.date, COUNT(q3.date) FROM source ALIAS q1 JOIN (SELECT party, COUNT(*) FROM source WHERE (CONTAINS('republican') BIND party OR CONTAINS('democrat') BIND party) AND NER(PERSON) BIND person GRANULARITY SENTENCE) ALIAS q2 ON q1.pres = q2.person JOIN (SELECT TITLE, person, date FROM source WHERE (CONTAINS('inaugurated') OR CONTAINS('took office')) AND NER(PERSON) BIND person AND DATE(INTERSECT [1900, 2000]) BIND date GRANULARITY SENTENCE) ALIAS q3 ON q2.person = q3.person WHERE DEPENDS('president','amod', '40th') AND CONTAINS('united states') AND NER(ORDINAL) BIND ordinal AND NER(PERSON) BIND pres GROUP BY q1.ordinal, q1.pres, q2.party, q3.date GRANULARITY SENTENCE
SELECT q1.ordinal, q1.pres, q2.party, q3.date, COUNT(q3.date) FROM source ALIAS q1 JOIN (SELECT party, COUNT(*) FROM source WHERE (CONTAINS('republican') BIND party OR CONTAINS('democrat') BIND party) AND NER(PERSON) BIND person GRANULARITY SENTENCE) ALIAS q2 ON q1.pres = q2.person JOIN (SELECT TITLE, person, date FROM source WHERE (CONTAINS('inaugurated') OR CONTAINS('took office')) AND NER(PERSON) BIND person AND DATE(INTERSECT [1900, 2000]) BIND date GRANULARITY SENTENCE) ALIAS q3 ON q2.person = q3.person WHERE DEPENDS('president','amod', '36th') AND CONTAINS('united states') AND NER(ORDINAL) BIND ordinal AND NER(PERSON) BIND pres GROUP BY q1.ordinal, q1.pres, q2.party, q3.date GRANULARITY SENTENCE
SELECT q1.ordinal, q1.pres, q2.party, q3.date, COUNT(q3.date) FROM source ALIAS q1 JOIN (SELECT party, COUNT(*) FROM source WHERE (CONTAINS('democrat') BIND party OR CONTAINS('republican') BIND party) AND NER(PERSON) BIND person GRANULARITY SENTENCE) ALIAS q2 ON q1.pres = q2.person JOIN (SELECT TITLE, person, date FROM source WHERE (CONTAINS('inaugurated') OR CONTAINS('took office')) AND NER(PERSON) BIND person AND DATE(INTERSECT [1900, 2000]) BIND date GRANULARITY SENTENCE) ALIAS q3 ON q2.person = q3.person WHERE DEPENDS('president','amod', '41st') AND CONTAINS('united states') AND NER(ORDINAL) BIND ordinal AND NER(PERSON) BIND pres GROUP BY q1.ordinal, q1.pres, q2.party, q3.date GRANULARITY SENTENCE
SELECT q1.ordinal, q1.pres, q2.party, q3.date, COUNT(q3.date) FROM source ALIAS q1 JOIN (SELECT party, COUNT(*) FROM source WHERE (CONTAINS('republican') BIND party OR CONTAINS('democrat') BIND party) AND NER(PERSON) BIND person GRANULARITY SENTENCE) ALIAS q2 ON q1.pres = q2.person JOIN (SELECT TITLE, person, date FROM source WHERE (CONTAINS('inaugurated') OR CONTAINS('took office')) AND NER(PERSON) BIND person AND DATE(INTERSECT [1950, 2007]) BIND date GRANULARITY SENTENCE) ALIAS q3 ON q2.person = q3.person WHERE DEPENDS('president','amod', '43rd') AND CONTAINS('united states') AND NER(ORDINAL) BIND ordinal AND NER(PERSON) BIND pres GROUP BY q1.ordinal, q1.pres, q2.party, q3.date GRANULARITY SENTENCE


#3hop, nth pres, name, party, death date
SELECT q1.ordinal, q1.pres, q2.party, q3.date, COUNT(q3.date) FROM source ALIAS q1 JOIN (SELECT party, COUNT(*) FROM source WHERE (CONTAINS('democrat') BIND party OR CONTAINS('republican') BIND party) AND NER(PERSON) BIND person GRANULARITY SENTENCE) ALIAS q2 ON q1.pres = q2.person JOIN (SELECT TITLE, person, date FROM source WHERE (CONTAINS('dies')) AND NER(PERSON) BIND person AND DATE(< 2050) BIND date GRANULARITY SENTENCE) ALIAS q3 ON q2.person = q3.person WHERE DEPENDS('president','amod', '38th') AND CONTAINS('united states') AND NER(ORDINAL) BIND ordinal AND NER(PERSON) BIND pres GROUP BY q1.ordinal, q1.pres, q2.party, q3.date GRANULARITY SENTENCE
SELECT q1.ordinal, q1.pres, q2.party, q3.date, COUNT(q3.date) FROM source ALIAS q1 JOIN (SELECT party, COUNT(*) FROM source WHERE (CONTAINS('democrat') BIND party OR CONTAINS('republican') BIND party) AND NER(PERSON) BIND person GRANULARITY SENTENCE) ALIAS q2 ON q1.pres = q2.person JOIN (SELECT TITLE, person, date FROM source WHERE (CONTAINS('dies')) AND NER(PERSON) BIND person AND DATE(< 2050) BIND date GRANULARITY SENTENCE) ALIAS q3 ON q2.person = q3.person WHERE DEPENDS('president','amod', *) AND CONTAINS('united states') AND NER(ORDINAL) BIND ordinal AND NER(PERSON) BIND pres GROUP BY q1.ordinal, q1.pres, q2.party, q3.date GRANULARITY SENTENCE

# astronaut date, country
SELECT q2.person, q3.country FROM source ALIAS q1 JOIN ( SELECT person, date FROM source WHERE CONTAINS('astronaut') AND NER(PERSON) BIND person AND DATE(< 2050) BIND date GROUP BY person, date GRANULARITY SENTENCE ) ALIAS q2 ON q1.date BEFORE q2.date JOIN ( SELECT person, country FROM source WHERE NER(NATIONALITY) BIND country AND NER(PERSON) BIND person GRANULARITY SENTENCE ) ALIAS q3 ON q2.person = q3.person WHERE CONTAINS('moon') AND NER(PERSON, 'Neil Armstrong') BIND person AND DATE(< 2050) BIND date GROUP BY q2.person, q3.country GRANULARITY SENTENCE
# astro after Yuri
SELECT q2.person, q3.organizationName FROM source ALIAS q1 JOIN ( SELECT person, date FROM source WHERE (CONTAINS('astronaut') OR CONTAINS('cosmonaut')) AND NER(PERSON) BIND person AND DATE(< 1980) BIND date GRANULARITY SENTENCE ) ALIAS q2 ON q1.date BEFORE q2.date JOIN ( SELECT person, organizationName FROM source WHERE NER(ORGANIZATION) BIND organizationName AND NER(PERSON) BIND person GRANULARITY SENTENCE ) ALIAS q3 ON q2.person = q3.person WHERE (CONTAINS('first human space') OR CONTAINS('Vostok 1')) AND NER(PERSON, 'Yuri Gagarin') BIND person AND DATE(< 1970) BIND date GROUP BY q2.person, q3.organizationName GRANULARITY SENTENCE


# after einstein, org
SELECT q2.personName, q2.awardDate, q3.organizationName, COUNT(*) FROM articles ALIAS q1 JOIN (SELECT personName, awardDate FROM articles WHERE CONTAINS('Nobel Prize') AND NER(PERSON) BIND personName AND DATE(< 2050) BIND awardDate GRANULARITY SENTENCE) ALIAS q2 ON q1.specificWinnerDate BEFORE q2.awardDate JOIN (SELECT personName, organizationName FROM articles WHERE NER(PERSON) BIND personName AND NER(ORGANIZATION) BIND organizationName GRANULARITY SENTENCE) ALIAS q3 ON q2.personName = q3.personName WHERE CONTAINS('Nobel Prize') AND NER(PERSON, 'Albert Einstein') BIND specificWinnerName AND DATE(< 2050) BIND specificWinnerDate GROUP BY q2.personName, q2.awardDate, q3.organizationName GRANULARITY SENTENCE ORDER BY COUNT(*) DESC


# after Marie Curie
SELECT q2.personName, q2.awardDate, q3.locationName, COUNT(*) FROM articles ALIAS q1 JOIN (SELECT personName, awardDate FROM articles WHERE CONTAINS('Nobel Prize') AND NER(PERSON) BIND personName AND DATE(< 2050) BIND awardDate GRANULARITY SENTENCE) ALIAS q2 ON q1.specificWinnerDate BEFORE q2.awardDate JOIN (SELECT personName, locationName FROM articles WHERE NER(PERSON) BIND personName AND NER(LOCATION) BIND locationName GRANULARITY SENTENCE) ALIAS q3 ON q2.personName = q3.personName WHERE CONTAINS('Nobel Prize') AND NER(PERSON, 'Marie Curie') BIND specificWinnerName AND DATE(< 2050) BIND specificWinnerDate GROUP BY q2.personName, q2.awardDate, q3.locationName GRANULARITY SENTENCE ORDER BY COUNT(*) DESC

SELECT q1.nobelWinner, q1.nobelDate, q2.countryName, q3.organizationName, COUNT(*) FROM articles ALIAS q1 JOIN (SELECT personName, countryName FROM articles WHERE NER(NATIONALITY) BIND countryName AND NER(PERSON) BIND personName GROUP BY personName, countryName GRANULARITY SENTENCE) ALIAS q2 ON q1.nobelWinner = q2.personName JOIN (SELECT personName, organizationName FROM articles WHERE CONTAINS('Nobel Prize') AND NER(ORGANIZATION) BIND organizationName AND NER(PERSON) BIND personName GROUP BY personName, organizationName GRANULARITY SENTENCE) ALIAS q3 ON q2.personName = q3.personName WHERE CONTAINS('Nobel Prize') AND NER(PERSON) BIND nobelWinner AND DATE(= 1998) BIND nobelDate GROUP BY q1.nobelWinner, q1.nobelDate, q2.countryName, q3.organizationName GRANULARITY SENTENCE ORDER BY COUNT(*) DESC
SELECT q1.nobelWinner, q1.nobelDate, q2.countryName, q3.organizationName, COUNT(*) FROM articles ALIAS q1 JOIN (SELECT personName, countryName FROM articles WHERE NER(NATIONALITY) BIND countryName AND NER(PERSON) BIND personName GROUP BY personName, countryName GRANULARITY SENTENCE) ALIAS q2 ON q1.nobelWinner = q2.personName JOIN (SELECT personName, organizationName FROM articles WHERE CONTAINS('Nobel Prize') AND NER(ORGANIZATION) BIND organizationName AND NER(PERSON) BIND personName GROUP BY personName, organizationName GRANULARITY SENTENCE) ALIAS q3 ON q2.personName = q3.personName WHERE CONTAINS('Nobel Prize') AND NER(PERSON) BIND nobelWinner AND DATE(= 1994) BIND nobelDate GROUP BY q1.nobelWinner, q1.nobelDate, q2.countryName, q3.organizationName GRANULARITY SENTENCE ORDER BY COUNT(*) DESC
SELECT q1.nobelWinner, q1.nobelDate, q2.countryName, q3.organizationName, COUNT(*) FROM articles ALIAS q1 JOIN (SELECT personName, countryName FROM articles WHERE NER(NATIONALITY) BIND countryName AND NER(PERSON) BIND personName GROUP BY personName, countryName GRANULARITY SENTENCE) ALIAS q2 ON q1.nobelWinner = q2.personName JOIN (SELECT personName, organizationName FROM articles WHERE CONTAINS('Nobel Prize') AND NER(ORGANIZATION) BIND organizationName AND NER(PERSON) BIND personName GROUP BY personName, organizationName GRANULARITY SENTENCE) ALIAS q3 ON q2.personName = q3.personName WHERE CONTAINS('Nobel Prize') AND NER(PERSON) BIND nobelWinner AND DATE(= 2001) BIND nobelDate GROUP BY q1.nobelWinner, q1.nobelDate, q2.countryName, q3.organizationName GRANULARITY SENTENCE ORDER BY COUNT(*) DESC
SELECT q1.nobelWinner, q1.nobelDate, q2.countryName, q3.organizationName, COUNT(*) FROM articles ALIAS q1 JOIN (SELECT personName, countryName FROM articles WHERE NER(NATIONALITY) BIND countryName AND NER(PERSON) BIND personName GROUP BY personName, countryName GRANULARITY SENTENCE) ALIAS q2 ON q1.nobelWinner = q2.personName JOIN (SELECT personName, organizationName FROM articles WHERE CONTAINS('Nobel Prize') AND NER(ORGANIZATION) BIND organizationName AND NER(PERSON) BIND personName GROUP BY personName, organizationName GRANULARITY SENTENCE) ALIAS q3 ON q2.personName = q3.personName WHERE CONTAINS('Nobel Prize') AND DATE(= 2004) BIND nobelDate AND NER(PERSON) BIND nobelWinner GROUP BY q1.nobelWinner, q1.nobelDate, q2.countryName, q3.organizationName GRANULARITY SENTENCE ORDER BY COUNT(*) DESC

# Earthquake, City Population, Country
SELECT q1.quakeLocation, q1.eventDate, q2.populationFigure, q3.countryName, COUNT(*) FROM articles ALIAS q1 JOIN (SELECT locationName, populationFigure FROM articles WHERE DEPENDS('residents', 'nummod', *) BIND populationFigure AND NER(CITY) BIND locationName GROUP BY locationName, populationFigure GRANULARITY SENTENCE) ALIAS q2 ON q1.quakeLocation = q2.locationName JOIN (SELECT cityName, countryName FROM articles WHERE NER(CITY) BIND cityName AND NER(COUNTRY) BIND countryName GROUP BY cityName, countryName GRANULARITY SENTENCE) ALIAS q3 ON q2.locationName = q3.cityName WHERE CONTAINS('earthquake') AND NER(CITY) BIND quakeLocation AND DATE(INTERSECT [1985,1990]) BIND eventDate GROUP BY q1.quakeLocation, q1.eventDate, q2.populationFigure, q3.countryName GRANULARITY SENTENCE ORDER BY COUNT(*) DESC
SELECT q1.quakeLocation, q1.eventDate, q2.populationFigure, q3.countryName, COUNT(*) FROM articles ALIAS q1 JOIN (SELECT locationName, populationFigure FROM articles WHERE DEPENDS('residents', 'nummod', *) BIND populationFigure AND NER(CITY) BIND locationName GROUP BY locationName, populationFigure GRANULARITY SENTENCE) ALIAS q2 ON q1.quakeLocation = q2.locationName JOIN (SELECT cityName, countryName FROM articles WHERE NER(CITY) BIND cityName AND NER(COUNTRY) BIND countryName GROUP BY cityName, countryName GRANULARITY SENTENCE) ALIAS q3 ON q2.locationName = q3.cityName WHERE CONTAINS('earthquake') AND NER(CITY) BIND quakeLocation AND DATE(INTERSECT [1980,1985]) BIND eventDate GROUP BY q1.quakeLocation, q1.eventDate, q2.populationFigure, q3.countryName GRANULARITY SENTENCE ORDER BY COUNT(*) DESC
SELECT q1.quakeLocation, q1.eventDate, q2.populationFigure, q3.countryName, COUNT(*) FROM articles ALIAS q1 JOIN (SELECT locationName, populationFigure FROM articles WHERE DEPENDS('residents', 'nummod', *) BIND populationFigure AND NER(CITY) BIND locationName GROUP BY locationName, populationFigure GRANULARITY SENTENCE) ALIAS q2 ON q1.quakeLocation = q2.locationName JOIN (SELECT cityName, countryName FROM articles WHERE NER(CITY) BIND cityName AND NER(COUNTRY) BIND countryName GROUP BY cityName, countryName GRANULARITY SENTENCE) ALIAS q3 ON q2.locationName = q3.cityName WHERE CONTAINS('earthquake') AND NER(CITY) BIND quakeLocation AND DATE(INTERSECT [1990,1995]) BIND eventDate GROUP BY q1.quakeLocation, q1.eventDate, q2.populationFigure, q3.countryName GRANULARITY SENTENCE ORDER BY COUNT(*) DESC
SELECT q1.quakeLocation, q1.eventDate, q2.populationFigure, q3.countryName, COUNT(*) FROM articles ALIAS q1 JOIN (SELECT locationName, populationFigure FROM articles WHERE DEPENDS('residents', 'nummod', *) BIND populationFigure AND NER(CITY) BIND locationName GROUP BY locationName, populationFigure GRANULARITY SENTENCE) ALIAS q2 ON q1.quakeLocation = q2.locationName JOIN (SELECT cityName, countryName FROM articles WHERE NER(CITY) BIND cityName AND NER(COUNTRY) BIND countryName GROUP BY cityName, countryName GRANULARITY SENTENCE) ALIAS q3 ON q2.locationName = q3.cityName WHERE CONTAINS('earthquake') AND NER(CITY) BIND quakeLocation AND DATE(INTERSECT [1995,2000]) BIND eventDate GROUP BY q1.quakeLocation, q1.eventDate, q2.populationFigure, q3.countryName GRANULARITY SENTENCE ORDER BY COUNT(*) DESC

# Additional earthquake queries with different date ranges
SELECT q1.quakeLocation, q1.eventDate, q2.populationFigure, q3.countryName, COUNT(*) FROM articles ALIAS q1 JOIN (SELECT locationName, populationFigure FROM articles WHERE DEPENDS('residents', 'nummod', *) BIND populationFigure AND NER(CITY) BIND locationName GROUP BY locationName, populationFigure GRANULARITY SENTENCE) ALIAS q2 ON q1.quakeLocation = q2.locationName JOIN (SELECT cityName, countryName FROM articles WHERE NER(CITY) BIND cityName AND NER(COUNTRY) BIND countryName GROUP BY cityName, countryName GRANULARITY SENTENCE) ALIAS q3 ON q2.locationName = q3.cityName WHERE CONTAINS('earthquake') AND NER(CITY) BIND quakeLocation AND DATE(INTERSECT [2000,2005]) BIND eventDate GROUP BY q1.quakeLocation, q1.eventDate, q2.populationFigure, q3.countryName GRANULARITY SENTENCE ORDER BY COUNT(*) DESC
SELECT q1.quakeLocation, q1.eventDate, q2.populationFigure, q3.countryName, COUNT(*) FROM articles ALIAS q1 JOIN (SELECT locationName, populationFigure FROM articles WHERE DEPENDS('residents', 'nummod', *) BIND populationFigure AND NER(CITY) BIND locationName GROUP BY locationName, populationFigure GRANULARITY SENTENCE) ALIAS q2 ON q1.quakeLocation = q2.locationName JOIN (SELECT cityName, countryName FROM articles WHERE NER(CITY) BIND cityName AND NER(COUNTRY) BIND countryName GROUP BY cityName, countryName GRANULARITY SENTENCE) ALIAS q3 ON q2.locationName = q3.cityName WHERE CONTAINS('earthquake') AND NER(CITY) BIND quakeLocation AND DATE(INTERSECT [1975,1985]) BIND eventDate GROUP BY q1.quakeLocation, q1.eventDate, q2.populationFigure, q3.countryName GRANULARITY SENTENCE ORDER BY COUNT(*) DESC
SELECT q1.quakeLocation, q1.eventDate, q2.populationFigure, q3.countryName, COUNT(*) FROM articles ALIAS q1 JOIN (SELECT locationName, populationFigure FROM articles WHERE DEPENDS('residents', 'nummod', *) BIND populationFigure AND NER(CITY) BIND locationName GROUP BY locationName, populationFigure GRANULARITY SENTENCE) ALIAS q2 ON q1.quakeLocation = q2.locationName JOIN (SELECT cityName, countryName FROM articles WHERE NER(CITY) BIND cityName AND NER(COUNTRY) BIND countryName GROUP BY cityName, countryName GRANULARITY SENTENCE) ALIAS q3 ON q2.locationName = q3.cityName WHERE CONTAINS('earthquake') AND NER(CITY) BIND quakeLocation AND DATE(INTERSECT [1970,1980]) BIND eventDate GROUP BY q1.quakeLocation, q1.eventDate, q2.populationFigure, q3.countryName GRANULARITY SENTENCE ORDER BY COUNT(*) DESC

# Additional Nobel Prize, nationality, organization queries with different years
SELECT q1.nobelWinner, q1.nobelDate, q2.countryName, q3.organizationName, COUNT(*) FROM articles ALIAS q1 JOIN (SELECT personName, countryName FROM articles WHERE NER(NATIONALITY) BIND countryName AND NER(PERSON) BIND personName GROUP BY personName, countryName GRANULARITY SENTENCE) ALIAS q2 ON q1.nobelWinner = q2.personName JOIN (SELECT personName, organizationName FROM articles WHERE CONTAINS('Nobel Prize') AND NER(ORGANIZATION) BIND organizationName AND NER(PERSON) BIND personName GROUP BY personName, organizationName GRANULARITY SENTENCE) ALIAS q3 ON q2.personName = q3.personName WHERE CONTAINS('Nobel Prize') AND DATE(= 1980) BIND nobelDate AND NER(PERSON) BIND nobelWinner GROUP BY q1.nobelWinner, q1.nobelDate, q2.countryName, q3.organizationName GRANULARITY SENTENCE ORDER BY COUNT(*) DESC
SELECT q1.nobelWinner, q1.nobelDate, q2.countryName, q3.organizationName, COUNT(*) FROM articles ALIAS q1 JOIN (SELECT personName, countryName FROM articles WHERE NER(NATIONALITY) BIND countryName AND NER(PERSON) BIND personName GROUP BY personName, countryName GRANULARITY SENTENCE) ALIAS q2 ON q1.nobelWinner = q2.personName JOIN (SELECT personName, organizationName FROM articles WHERE CONTAINS('Nobel Prize') AND NER(ORGANIZATION) BIND organizationName AND NER(PERSON) BIND personName GROUP BY personName, organizationName GRANULARITY SENTENCE) ALIAS q3 ON q2.personName = q3.personName WHERE CONTAINS('Nobel Prize') AND DATE(= 1985) BIND nobelDate AND NER(PERSON) BIND nobelWinner GROUP BY q1.nobelWinner, q1.nobelDate, q2.countryName, q3.organizationName GRANULARITY SENTENCE ORDER BY COUNT(*) DESC
SELECT q1.nobelWinner, q1.nobelDate, q2.countryName, q3.organizationName, COUNT(*) FROM articles ALIAS q1 JOIN (SELECT personName, countryName FROM articles WHERE NER(NATIONALITY) BIND countryName AND NER(PERSON) BIND personName GROUP BY personName, countryName GRANULARITY SENTENCE) ALIAS q2 ON q1.nobelWinner = q2.personName JOIN (SELECT personName, organizationName FROM articles WHERE CONTAINS('Nobel Prize') AND NER(ORGANIZATION) BIND organizationName AND NER(PERSON) BIND personName GROUP BY personName, organizationName GRANULARITY SENTENCE) ALIAS q3 ON q2.personName = q3.personName WHERE CONTAINS('Nobel Prize') AND DATE(= 1990) BIND nobelDate AND NER(PERSON) BIND nobelWinner GROUP BY q1.nobelWinner, q1.nobelDate, q2.countryName, q3.organizationName GRANULARITY SENTENCE ORDER BY COUNT(*) DESC
SELECT q1.nobelWinner, q1.nobelDate, q2.countryName, q3.organizationName, COUNT(*) FROM articles ALIAS q1 JOIN (SELECT personName, countryName FROM articles WHERE NER(NATIONALITY) BIND countryName AND NER(PERSON) BIND personName GROUP BY personName, countryName GRANULARITY SENTENCE) ALIAS q2 ON q1.nobelWinner = q2.personName JOIN (SELECT personName, organizationName FROM articles WHERE CONTAINS('Nobel Prize') AND NER(ORGANIZATION) BIND organizationName AND NER(PERSON) BIND personName GROUP BY personName, organizationName GRANULARITY SENTENCE) ALIAS q3 ON q2.personName = q3.personName WHERE CONTAINS('Nobel Prize') AND DATE(= 1995) BIND nobelDate AND NER(PERSON) BIND nobelWinner GROUP BY q1.nobelWinner, q1.nobelDate, q2.countryName, q3.organizationName GRANULARITY SENTENCE ORDER BY COUNT(*) DESC
SELECT q1.nobelWinner, q1.nobelDate, q2.countryName, q3.organizationName, COUNT(*) FROM articles ALIAS q1 JOIN (SELECT personName, countryName FROM articles WHERE NER(NATIONALITY) BIND countryName AND NER(PERSON) BIND personName GROUP BY personName, countryName GRANULARITY SENTENCE) ALIAS q2 ON q1.nobelWinner = q2.personName JOIN (SELECT personName, organizationName FROM articles WHERE CONTAINS('Nobel Prize') AND NER(ORGANIZATION) BIND organizationName AND NER(PERSON) BIND personName GROUP BY personName, organizationName GRANULARITY SENTENCE) ALIAS q3 ON q2.personName = q3.personName WHERE CONTAINS('Nobel Prize') AND DATE(= 2000) BIND nobelDate AND NER(PERSON) BIND nobelWinner GROUP BY q1.nobelWinner, q1.nobelDate, q2.countryName, q3.organizationName GRANULARITY SENTENCE ORDER BY COUNT(*) DESC
SELECT q1.nobelWinner, q1.nobelDate, q2.countryName, q3.organizationName, COUNT(*) FROM articles ALIAS q1 JOIN (SELECT personName, countryName FROM articles WHERE NER(NATIONALITY) BIND countryName AND NER(PERSON) BIND personName GROUP BY personName, countryName GRANULARITY SENTENCE) ALIAS q2 ON q1.nobelWinner = q2.personName JOIN (SELECT personName, organizationName FROM articles WHERE CONTAINS('Nobel Prize') AND NER(ORGANIZATION) BIND organizationName AND NER(PERSON) BIND personName GROUP BY personName, organizationName GRANULARITY SENTENCE) ALIAS q3 ON q2.personName = q3.personName WHERE CONTAINS('Nobel Prize') AND DATE(= 2005) BIND nobelDate AND NER(PERSON) BIND nobelWinner GROUP BY q1.nobelWinner, q1.nobelDate, q2.countryName, q3.organizationName GRANULARITY SENTENCE ORDER BY COUNT(*) DESC

# Athletes, nationalities and organizations for different sports
SELECT q1.athleteName, q1.eventDate, q2.countryName, q3.organizationName, COUNT(*) FROM articles ALIAS q1 JOIN (SELECT personName, countryName FROM articles WHERE NER(NATIONALITY) BIND countryName AND NER(PERSON) BIND personName GROUP BY personName, countryName GRANULARITY SENTENCE) ALIAS q2 ON q1.athleteName = q2.personName JOIN (SELECT personName, organizationName FROM articles WHERE CONTAINS('Olympics') AND NER(ORGANIZATION) BIND organizationName AND NER(PERSON) BIND personName GROUP BY personName, organizationName GRANULARITY SENTENCE) ALIAS q3 ON q2.personName = q3.personName WHERE CONTAINS('Olympics') AND NER(PERSON) BIND athleteName AND DATE(= 1984) BIND eventDate GROUP BY q1.athleteName, q1.eventDate, q2.countryName, q3.organizationName GRANULARITY SENTENCE ORDER BY COUNT(*) DESC
SELECT q1.athleteName, q1.eventDate, q2.countryName, q3.organizationName, COUNT(*) FROM articles ALIAS q1 JOIN (SELECT personName, countryName FROM articles WHERE NER(NATIONALITY) BIND countryName AND NER(PERSON) BIND personName GROUP BY personName, countryName GRANULARITY SENTENCE) ALIAS q2 ON q1.athleteName = q2.personName JOIN (SELECT personName, organizationName FROM articles WHERE CONTAINS('Olympics') AND NER(ORGANIZATION) BIND organizationName AND NER(PERSON) BIND personName GROUP BY personName, organizationName GRANULARITY SENTENCE) ALIAS q3 ON q2.personName = q3.personName WHERE CONTAINS('Olympics') AND NER(PERSON) BIND athleteName AND DATE(= 1988) BIND eventDate GROUP BY q1.athleteName, q1.eventDate, q2.countryName, q3.organizationName GRANULARITY SENTENCE ORDER BY COUNT(*) DESC
SELECT q1.athleteName, q1.eventDate, q2.countryName, q3.organizationName, COUNT(*) FROM articles ALIAS q1 JOIN (SELECT personName, countryName FROM articles WHERE NER(NATIONALITY) BIND countryName AND NER(PERSON) BIND personName GROUP BY personName, countryName GRANULARITY SENTENCE) ALIAS q2 ON q1.athleteName = q2.personName JOIN (SELECT personName, organizationName FROM articles WHERE CONTAINS('Olympics') AND NER(ORGANIZATION) BIND organizationName AND NER(PERSON) BIND personName GROUP BY personName, organizationName GRANULARITY SENTENCE) ALIAS q3 ON q2.personName = q3.personName WHERE CONTAINS('Olympics') AND NER(PERSON) BIND athleteName AND DATE(= 1992) BIND eventDate GROUP BY q1.athleteName, q1.eventDate, q2.countryName, q3.organizationName GRANULARITY SENTENCE ORDER BY COUNT(*) DESC
SELECT q1.athleteName, q1.eventDate, q2.countryName, q3.organizationName, COUNT(*) FROM articles ALIAS q1 JOIN (SELECT personName, countryName FROM articles WHERE NER(NATIONALITY) BIND countryName AND NER(PERSON) BIND personName GROUP BY personName, countryName GRANULARITY SENTENCE) ALIAS q2 ON q1.athleteName = q2.personName JOIN (SELECT personName, organizationName FROM articles WHERE CONTAINS('Olympics') AND NER(ORGANIZATION) BIND organizationName AND NER(PERSON) BIND personName GROUP BY personName, organizationName GRANULARITY SENTENCE) ALIAS q3 ON q2.personName = q3.personName WHERE CONTAINS('Olympics') AND NER(PERSON) BIND athleteName AND DATE(= 1996) BIND eventDate GROUP BY q1.athleteName, q1.eventDate, q2.countryName, q3.organizationName GRANULARITY SENTENCE ORDER BY COUNT(*) DESC
SELECT q1.athleteName, q1.eventDate, q2.countryName, q3.organizationName, COUNT(*) FROM articles ALIAS q1 JOIN (SELECT personName, countryName FROM articles WHERE NER(NATIONALITY) BIND countryName AND NER(PERSON) BIND personName GROUP BY personName, countryName GRANULARITY SENTENCE) ALIAS q2 ON q1.athleteName = q2.personName JOIN (SELECT personName, organizationName FROM articles WHERE CONTAINS('Olympics') AND NER(ORGANIZATION) BIND organizationName AND NER(PERSON) BIND personName GROUP BY personName, organizationName GRANULARITY SENTENCE) ALIAS q3 ON q2.personName = q3.personName WHERE CONTAINS('Olympics') AND NER(PERSON) BIND athleteName AND DATE(= 2000) BIND eventDate GROUP BY q1.athleteName, q1.eventDate, q2.countryName, q3.organizationName GRANULARITY SENTENCE ORDER BY COUNT(*) DESC

# Musicians, nationalities and record labels
SELECT q1.musicianName, q1.performanceDate, q2.countryName, q3.organizationName, COUNT(*) FROM articles ALIAS q1 JOIN (SELECT personName, countryName FROM articles WHERE NER(NATIONALITY) BIND countryName AND NER(PERSON) BIND personName GROUP BY personName, countryName GRANULARITY SENTENCE) ALIAS q2 ON q1.musicianName = q2.personName JOIN (SELECT personName, organizationName FROM articles WHERE CONTAINS('record') AND NER(ORGANIZATION) BIND organizationName AND NER(PERSON) BIND personName GROUP BY personName, organizationName GRANULARITY SENTENCE) ALIAS q3 ON q2.personName = q3.personName WHERE CONTAINS('album') AND NER(PERSON) BIND musicianName AND DATE(INTERSECT [1960,1970]) BIND performanceDate GROUP BY q1.musicianName, q1.performanceDate, q2.countryName, q3.organizationName GRANULARITY SENTENCE ORDER BY COUNT(*) DESC

# Authors, nationalities and publishers
SELECT q1.authorName, q1.pubDate, q2.countryName, q3.organizationName, COUNT(*) FROM articles ALIAS q1 JOIN (SELECT personName, countryName FROM articles WHERE NER(NATIONALITY) BIND countryName AND NER(PERSON) BIND personName GROUP BY personName, countryName GRANULARITY SENTENCE) ALIAS q2 ON q1.authorName = q2.personName JOIN (SELECT personName, organizationName FROM articles WHERE CONTAINS('published') AND NER(ORGANIZATION) BIND organizationName AND NER(PERSON) BIND personName GROUP BY personName, organizationName GRANULARITY SENTENCE) ALIAS q3 ON q2.personName = q3.personName WHERE CONTAINS('published') AND NER(PERSON) BIND authorName AND DATE(INTERSECT [1900,1920]) BIND pubDate GROUP BY q1.authorName, q1.pubDate, q2.countryName, q3.organizationName GRANULARITY SENTENCE ORDER BY COUNT(*) DESC
SELECT q1.authorName, q1.pubDate, q2.countryName, q3.organizationName, COUNT(*) FROM articles ALIAS q1 JOIN (SELECT personName, countryName FROM articles WHERE NER(NATIONALITY) BIND countryName AND NER(PERSON) BIND personName GROUP BY personName, countryName GRANULARITY SENTENCE) ALIAS q2 ON q1.authorName = q2.personName JOIN (SELECT personName, organizationName FROM articles WHERE CONTAINS('published') AND NER(ORGANIZATION) BIND organizationName AND NER(PERSON) BIND personName GROUP BY personName, organizationName GRANULARITY SENTENCE) ALIAS q3 ON q2.personName = q3.personName WHERE CONTAINS('novelist') AND NER(PERSON) BIND authorName AND DATE(INTERSECT [1920,1940]) BIND pubDate GROUP BY q1.authorName, q1.pubDate, q2.countryName, q3.organizationName GRANULARITY SENTENCE ORDER BY COUNT(*) DESC
SELECT q1.authorName, q1.pubDate, q2.countryName, q3.organizationName, COUNT(*) FROM articles ALIAS q1 JOIN (SELECT personName, countryName FROM articles WHERE NER(NATIONALITY) BIND countryName AND NER(PERSON) BIND personName GROUP BY personName, countryName GRANULARITY SENTENCE) ALIAS q2 ON q1.authorName = q2.personName JOIN (SELECT personName, organizationName FROM articles WHERE CONTAINS('published') AND NER(ORGANIZATION) BIND organizationName AND NER(PERSON) BIND personName GROUP BY personName, organizationName GRANULARITY SENTENCE) ALIAS q3 ON q2.personName = q3.personName WHERE CONTAINS('poet') AND NER(PERSON) BIND authorName AND DATE(INTERSECT [1940,1960]) BIND pubDate GROUP BY q1.authorName, q1.pubDate, q2.countryName, q3.organizationName GRANULARITY SENTENCE ORDER BY COUNT(*) DESC
SELECT q1.authorName, q1.pubDate, q2.countryName, q3.organizationName, COUNT(*) FROM articles ALIAS q1 JOIN (SELECT personName, countryName FROM articles WHERE NER(NATIONALITY) BIND countryName AND NER(PERSON) BIND personName GROUP BY personName, countryName GRANULARITY SENTENCE) ALIAS q2 ON q1.authorName = q2.personName JOIN (SELECT personName, organizationName FROM articles WHERE CONTAINS('published') AND NER(ORGANIZATION) BIND organizationName AND NER(PERSON) BIND personName GROUP BY personName, organizationName GRANULARITY SENTENCE) ALIAS q3 ON q2.personName = q3.personName WHERE CONTAINS('author') AND NER(PERSON) BIND authorName AND DATE(INTERSECT [1960,1980]) BIND pubDate GROUP BY q1.authorName, q1.pubDate, q2.countryName, q3.organizationName GRANULARITY SENTENCE ORDER BY COUNT(*) DESC

# Actors, nationalities and movie studios
SELECT q1.actorName, q1.movieDate, q2.countryName, q3.organizationName, COUNT(*) FROM articles ALIAS q1 JOIN (SELECT personName, countryName FROM articles WHERE NER(NATIONALITY) BIND countryName AND NER(PERSON) BIND personName GROUP BY personName, countryName GRANULARITY SENTENCE) ALIAS q2 ON q1.actorName = q2.personName JOIN (SELECT personName, organizationName FROM articles WHERE CONTAINS('studio') AND NER(ORGANIZATION) BIND organizationName AND NER(PERSON) BIND personName GROUP BY personName, organizationName GRANULARITY SENTENCE) ALIAS q3 ON q2.personName = q3.personName WHERE CONTAINS('director') AND NER(PERSON) BIND actorName AND DATE(INTERSECT [1960,1970]) BIND movieDate GROUP BY q1.actorName, q1.movieDate, q2.countryName, q3.organizationName GRANULARITY SENTENCE ORDER BY COUNT(*) DESC

# Explorers, nationalities and discoveries
SELECT q1.explorerName, q1.expeditionDate, q2.countryName, q3.discoveryName, COUNT(*) FROM articles ALIAS q1 JOIN (SELECT personName, countryName FROM articles WHERE NER(NATIONALITY) BIND countryName AND NER(PERSON) BIND personName GROUP BY personName, countryName GRANULARITY SENTENCE) ALIAS q2 ON q1.explorerName = q2.personName JOIN (SELECT personName, discoveryName FROM articles WHERE CONTAINS('discovered') AND NER(PERSON) BIND personName AND NER(LOCATION) BIND discoveryName GROUP BY personName, discoveryName GRANULARITY SENTENCE) ALIAS q3 ON q2.personName = q3.personName WHERE CONTAINS('explorer') AND NER(PERSON) BIND explorerName AND DATE(INTERSECT [1400,1600]) BIND expeditionDate GROUP BY q1.explorerName, q1.expeditionDate, q2.countryName, q3.discoveryName GRANULARITY SENTENCE ORDER BY COUNT(*) DESC
SELECT q1.explorerName, q1.expeditionDate, q2.countryName, q3.discoveryName, COUNT(*) FROM articles ALIAS q1 JOIN (SELECT personName, countryName FROM articles WHERE NER(NATIONALITY) BIND countryName AND NER(PERSON) BIND personName GROUP BY personName, countryName GRANULARITY SENTENCE) ALIAS q2 ON q1.explorerName = q2.personName JOIN (SELECT personName, discoveryName FROM articles WHERE CONTAINS('voyage') AND NER(PERSON) BIND personName AND NER(LOCATION) BIND discoveryName GROUP BY personName, discoveryName GRANULARITY SENTENCE) ALIAS q3 ON q2.personName = q3.personName WHERE CONTAINS('expedition') AND NER(PERSON) BIND explorerName AND DATE(INTERSECT [1600,1800]) BIND expeditionDate GROUP BY q1.explorerName, q1.expeditionDate, q2.countryName, q3.discoveryName GRANULARITY SENTENCE ORDER BY COUNT(*) DESC
SELECT q1.explorerName, q1.expeditionDate, q2.countryName, q3.discoveryName, COUNT(*) FROM articles ALIAS q1 JOIN (SELECT personName, countryName FROM articles WHERE NER(NATIONALITY) BIND countryName AND NER(PERSON) BIND personName GROUP BY personName, countryName GRANULARITY SENTENCE) ALIAS q2 ON q1.explorerName = q2.personName JOIN (SELECT personName, discoveryName FROM articles WHERE CONTAINS('discovered') AND NER(PERSON) BIND personName AND NER(LOCATION) BIND discoveryName GROUP BY personName, discoveryName GRANULARITY SENTENCE) ALIAS q3 ON q2.personName = q3.personName WHERE CONTAINS('navigator') AND NER(PERSON) BIND explorerName AND DATE(INTERSECT [1500,1700]) BIND expeditionDate GROUP BY q1.explorerName, q1.expeditionDate, q2.countryName, q3.discoveryName GRANULARITY SENTENCE ORDER BY COUNT(*) DESC

# Medical researchers, nationalities and institutions
SELECT q1.researcherName, q1.discoveryDate, q2.countryName, q3.organizationName, COUNT(*) FROM articles ALIAS q1 JOIN (SELECT personName, countryName FROM articles WHERE NER(NATIONALITY) BIND countryName AND NER(PERSON) BIND personName GROUP BY personName, countryName GRANULARITY SENTENCE) ALIAS q2 ON q1.researcherName = q2.personName JOIN (SELECT personName, organizationName FROM articles WHERE CONTAINS('hospital') AND NER(ORGANIZATION) BIND organizationName AND NER(PERSON) BIND personName GROUP BY personName, organizationName GRANULARITY SENTENCE) ALIAS q3 ON q2.personName = q3.personName WHERE CONTAINS('vaccine') AND NER(PERSON) BIND researcherName AND DATE(INTERSECT [1950,1970]) BIND discoveryDate GROUP BY q1.researcherName, q1.discoveryDate, q2.countryName, q3.organizationName GRANULARITY SENTENCE ORDER BY COUNT(*) DESC
SELECT q1.researcherName, q1.discoveryDate, q2.countryName, q3.organizationName, COUNT(*) FROM articles ALIAS q1 JOIN (SELECT personName, countryName FROM articles WHERE NER(NATIONALITY) BIND countryName AND NER(PERSON) BIND personName GROUP BY personName, countryName GRANULARITY SENTENCE) ALIAS q2 ON q1.researcherName = q2.personName JOIN (SELECT personName, organizationName FROM articles WHERE CONTAINS('medical school') AND NER(ORGANIZATION) BIND organizationName AND NER(PERSON) BIND personName GROUP BY personName, organizationName GRANULARITY SENTENCE) ALIAS q3 ON q2.personName = q3.personName WHERE CONTAINS('treatment') AND NER(PERSON) BIND researcherName AND DATE(INTERSECT [1970,1990]) BIND discoveryDate GROUP BY q1.researcherName, q1.discoveryDate, q2.countryName, q3.organizationName GRANULARITY SENTENCE ORDER BY COUNT(*) DESC
SELECT q1.researcherName, q1.discoveryDate, q2.countryName, q3.organizationName, COUNT(*) FROM articles ALIAS q1 JOIN (SELECT personName, countryName FROM articles WHERE NER(NATIONALITY) BIND countryName AND NER(PERSON) BIND personName GROUP BY personName, countryName GRANULARITY SENTENCE) ALIAS q2 ON q1.researcherName = q2.personName JOIN (SELECT personName, organizationName FROM articles WHERE CONTAINS('clinic') AND NER(ORGANIZATION) BIND organizationName AND NER(PERSON) BIND personName GROUP BY personName, organizationName GRANULARITY SENTENCE) ALIAS q3 ON q2.personName = q3.personName WHERE CONTAINS('medicine') AND NER(PERSON) BIND researcherName AND DATE(INTERSECT [1980,2000]) BIND discoveryDate GROUP BY q1.researcherName, q1.discoveryDate, q2.countryName, q3.organizationName GRANULARITY SENTENCE ORDER BY COUNT(*) DESC

# Business leaders, companies and industries for different eras
SELECT q1.businessLeader, q1.businessDate, q2.companyName, q3.industryType, COUNT(*) FROM articles ALIAS q1 JOIN (SELECT personName, companyName FROM articles WHERE CONTAINS('CEO') AND NER(PERSON) BIND personName AND NER(ORGANIZATION) BIND companyName GROUP BY personName, companyName GRANULARITY SENTENCE) ALIAS q2 ON q1.businessLeader = q2.personName JOIN (SELECT companyName, industryType FROM articles WHERE (CONTAINS('technology') BIND industryType OR CONTAINS('finance') BIND industryType OR CONTAINS('retail') BIND industryType) AND NER(ORGANIZATION) BIND companyName GROUP BY companyName, industryType GRANULARITY SENTENCE) ALIAS q3 ON q2.companyName = q3.companyName WHERE NER(PERSON) BIND businessLeader AND DATE(INTERSECT [1980,2000]) BIND businessDate GROUP BY q1.businessLeader, q1.businessDate, q2.companyName, q3.industryType GRANULARITY SENTENCE ORDER BY COUNT(*) DESC


# Artists, nationalities and art movements
SELECT q1.artistName, q1.artDate, q2.countryName, q3.artMovement, COUNT(*) FROM articles ALIAS q1 JOIN (SELECT personName, countryName FROM articles WHERE NER(NATIONALITY) BIND countryName AND NER(PERSON) BIND personName GROUP BY personName, countryName GRANULARITY SENTENCE) ALIAS q2 ON q1.artistName = q2.personName JOIN (SELECT personName, artMovement FROM articles WHERE (CONTAINS('Impressionism') BIND artMovement OR CONTAINS('Cubism') BIND artMovement OR CONTAINS('Surrealism') BIND artMovement) AND NER(PERSON) BIND personName GROUP BY personName, artMovement GRANULARITY SENTENCE) ALIAS q3 ON q2.personName = q3.personName WHERE CONTAINS('painter') AND NER(PERSON) BIND artistName AND DATE(INTERSECT [1850,1900]) BIND artDate GROUP BY q1.artistName, q1.artDate, q2.countryName, q3.artMovement GRANULARITY SENTENCE ORDER BY COUNT(*) DESC
SELECT q1.artistName, q1.artDate, q2.countryName, q3.artMovement, COUNT(*) FROM articles ALIAS q1 JOIN (SELECT personName, countryName FROM articles WHERE NER(NATIONALITY) BIND countryName AND NER(PERSON) BIND personName GROUP BY personName, countryName GRANULARITY SENTENCE) ALIAS q2 ON q1.artistName = q2.personName JOIN (SELECT personName, artMovement FROM articles WHERE (CONTAINS('Abstract') BIND artMovement OR CONTAINS('Expressionism') BIND artMovement OR CONTAINS('Modernism') BIND artMovement) AND NER(PERSON) BIND personName GROUP BY personName, artMovement GRANULARITY SENTENCE) ALIAS q3 ON q2.personName = q3.personName WHERE CONTAINS('sculptor') AND NER(PERSON) BIND artistName AND DATE(INTERSECT [1900,1950]) BIND artDate GROUP BY q1.artistName, q1.artDate, q2.countryName, q3.artMovement GRANULARITY SENTENCE ORDER BY COUNT(*) DESC

# Educational institutions, founders and academic fields
SELECT q1.institutionName, q1.foundedDate, q2.founderName, q3.academicField, COUNT(*) FROM articles ALIAS q1 JOIN (SELECT institutionName, founderName FROM articles WHERE CONTAINS('university') AND NER(ORGANIZATION) BIND institutionName AND NER(PERSON) BIND founderName GROUP BY institutionName, founderName GRANULARITY SENTENCE) ALIAS q2 ON q1.institutionName = q2.institutionName JOIN (SELECT founderName, academicField FROM articles WHERE (CONTAINS('mathematics') BIND academicField OR CONTAINS('physics') BIND academicField OR CONTAINS('philosophy') BIND academicField) AND NER(PERSON) BIND founderName GROUP BY founderName, academicField GRANULARITY SENTENCE) ALIAS q3 ON q2.founderName = q3.founderName WHERE NER(ORGANIZATION) BIND institutionName AND DATE(INTERSECT [1800,1900]) BIND foundedDate GROUP BY q1.institutionName, q1.foundedDate, q2.founderName, q3.academicField GRANULARITY SENTENCE ORDER BY COUNT(*) DESC
SELECT q1.institutionName, q1.foundedDate, q2.founderName, q3.academicField, COUNT(*) FROM articles ALIAS q1 JOIN (SELECT institutionName, founderName FROM articles WHERE CONTAINS('college') AND NER(ORGANIZATION) BIND institutionName AND NER(PERSON) BIND founderName GROUP BY institutionName, founderName GRANULARITY SENTENCE) ALIAS q2 ON q1.institutionName = q2.institutionName JOIN (SELECT founderName, academicField FROM articles WHERE (CONTAINS('medicine') BIND academicField OR CONTAINS('engineering') BIND academicField OR CONTAINS('theology') BIND academicField) AND NER(PERSON) BIND founderName GROUP BY founderName, academicField GRANULARITY SENTENCE) ALIAS q3 ON q2.founderName = q3.founderName WHERE NER(ORGANIZATION) BIND institutionName AND DATE(INTERSECT [1600,1800]) BIND foundedDate GROUP BY q1.institutionName, q1.foundedDate, q2.founderName, q3.academicField GRANULARITY SENTENCE ORDER BY COUNT(*) DESC