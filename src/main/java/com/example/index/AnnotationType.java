package com.example.index;

/**
 * Enum representing the different types of annotations that can be
 * associated with text in the stitch index.
 */
public enum AnnotationType {
    DATE,
    NER,
    POS,
    DEPENDENCY,
    UNKNOWN
}